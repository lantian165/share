nginx 安装
sudo yum install nginx

systemctl start nginx

# 默认配置文件存放路径
/etc/nginx/

# 网站默认根目录 在哪里配置
/etc/nginx/conf.d/default.conf
location -> root

默认情况下， 网站根目录是：
/usr/share/nginx/html

# 我们把phpmyadmin的安装文件放在这个路径下：
/usr/share/nginx/html/phpmyadmin

# 百度 nginx + phpmyadmin 发现配置方法比较特别
# 还涉及到 nginx + php的配置问题， 比较特殊，
# 先用最保险的 apache 服务器来使用phpmyadmin
